name: "Repo B CI"

on:
  workflow_call:
    inputs:
      name: 
        description: Event name
        required: true
        type: string
      data:
        description: Event data
        required: true
        type: string

jobs:
  execute:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Test
        run: cat test.txt

      - name: Log
        shell: bash
        run: |
          echo "${{ inputs.name }}"
